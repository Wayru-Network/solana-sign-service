export const ROUTES = {
  // API paths
  API: '/api',
  
  // Protected routes
  REQUEST_TRANSACTION: '/request-transaction',
  // You can add more protected routes here
  
  // Public routes (if you need them in the future)
  // PUBLIC_ROUTE: '/public',
} as const;